### 1. 这段代码的整体流程是什么？

让它按步骤解释。

### 2. 每个函数负责什么？
比如：
```python
load_model()
capture_screen()
preprocess()
infer()
postprocess()
show_result()
```
每个函数一句话解释。
### 3. 哪些代码是核心，哪些只是辅助？

你重点学核心代码，不要被全部代码吓住。
### 4. 如果报错，应该从哪里查？

让 AI 给你排查路径。
### 5. 面试官可能怎么问？

让 AI 帮你反向准备项目八股。