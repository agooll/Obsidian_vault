### 题目
![](assets/2958.%20最多%20K%20个重复元素的最长子数组/file-20260528174651007.png)
### 题解
最多K个重复元素的最长子数组 所以还是需要哈希map 一边去遍历数组 一边去拿哈希map的value跟K对比 当大于K的时候  可以删除left for里面计算ans

#### LeetCode写法
```cpp
class Solution {
public:
    int maxSubarrayLength(vector<int>& nums, int k) {
        unordered_map<int,int>mp;
        int n = nums.size();
        int left = 0;
        int ans = 0;
        for(int right= 0;right<n;right++){
            mp[nums[right]]++;
            while(mp[nums[right]]>k){
                mp[nums[left]]--;
                left++;
            }
            ans = max(ans,right-left+1);
        }
        return ans;
    }
};
```

## 图谱关联

- [[00_全库总览MOC]]
- [[算法精讲总览MOC]]
- [[LeetCode刷题总览MOC]]
- [[3.无重复字符最长字串]]
- [[209. 长度最小的子数组]]
- [[713. 乘积小于 K 的子数组]]
