### 题目
![](assets/1004.%20最大连续1的个数%20III/file-20260528235043851.png)
#### leetcode
哈希map
```cpp

class Solution {
public:
    int longestOnes(vector<int>& nums, int k) {
        unordered_map<int,int>mp;
        int n = nums.size();
        int ans = 0;
        int left = 0;
        for(int right = 0;right<n;right++){
            mp[nums[right]]++;
            while(mp[0]>k){
                mp[nums[left]]--;
                left++;
            }
            ans = max(ans,right-left + 1);
        }
        return ans;
    }
};
```
不用哈希
```cpp
class Solution {
public:
    int longestOnes(vector<int>& nums, int k) {
        int ans = 0,count0 = 0,left = 0;
        for(int right = 0;right<nums.size();right++){
            if(nums[right] == 0){
                count0 ++;
            }
            while(count0>k){
                if(nums[left] == 0){
                    count0--;
                }
                left++;
            }
            ans = max(ans,right- left +1);
        }
        return ans;
    }
};
```