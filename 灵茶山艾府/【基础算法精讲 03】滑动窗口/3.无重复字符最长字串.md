![](assets/3.无重复字符最长字串/file-20260525215014624.png)
### unordered_set 写法
```cpp
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        unordered_set<char>window;
        int ans = 0;
        int left = 0;
        int n = s.size();
        for(int right = 0; right < n;right++){
            while(window.count(s[right])){
                window.erase(s[left]);
                left++;
            }
            window.insert(s[right]);
            ans = max(ans,right - left + 1);
        }
        return ans;
    }
};
```
#### while(window.count(s[right])){}为什么可以作为一个判断条件
count是哈希set的一个API 由于哈希set的特性 他不能保存相同值 所以count的返回值只会是 0 或
1 所以这个的判断条件是window.count(s[right] 会不会等于 1 等于1 为true 即存在这个元素就为true
#### window.erase()的理解
这个是删除元素的API 