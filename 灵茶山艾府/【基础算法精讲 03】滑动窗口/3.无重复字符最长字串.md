![](assets/3.无重复字符最长字串/file-20260525215014624.png)
### unordered_set 写法
```cpp
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        unordered_set<char>window;
        int ans = 0;
        int left = 0;
        int n = s.size();
        for(int right = 0; right < n;right++){
            while(window.count(s[right])){
                window.erase(s[left]);
                left++;
            }
            window.insert(s[right]);
            ans = max(ans,right - left + 1);
        }
        return ans;
    }
};
```
#### while(window.count(s[right])){}为什么可以zuo'wei一个判断条件
