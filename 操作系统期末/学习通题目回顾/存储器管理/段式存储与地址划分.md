# 1. 段式存储与地址划分

- 第 11 题（错题）：题目给出了逻辑地址共 26 位。
  - 其中 8 位表示段号（也就是说最多可以有 2^8 个段）。
  - 那么剩下的位数全部留给段内地址（段长）：26 - 8 = 18 位。
  - 因此，段长最大是 2^18（答案为 C）。你当时可能误把 26 位当成了段内地址或者直接选了其它的边界值。

- 第 23 题（判断）：多分区存储管理中，一个分区通常只能装入一个作业或进程，不是“每个分区可存放多个进程”，所以题干错误。
