# 3. 设备（I/O）管理与缓冲

- 第 10 题：在轮询、中断、DMA、通道这四种 I/O 控制方式中，I/O 通道控制方式拥有自己的通道程序和专用的“通道处理器”，对 CPU 的干预要求最少。

- 第 20 题（判断）：DMA（直接内存存取）方式的精髓在于解放 CPU，让 I/O 设备（通过 DMA 控制器）与主存之间建立直接的数据通路。题干把通路说成建立在 CPU 和主存之间，所以错误。

- 第 21 题（判断）：引入缓冲技术（Buffer）就是为了缓和 CPU 和 I/O 设备之间速度不匹配的矛盾，从而提高它们之间的并行程度。
